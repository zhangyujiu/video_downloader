{
  "name": "douyin-downloader",
  "version": "1.0.0",
  "description": "Serverless Douyin Downloader running on Cloudflare Workers using Browser Rendering API",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.3.0"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
